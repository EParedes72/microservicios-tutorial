spring:
  application:
    name: usuario-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: "always"