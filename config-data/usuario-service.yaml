server:
  port: 8001
spring:
  application:
    name: usuario-service

#Actuator
management:
  endpoints:
    web:
      exposure:
        include: *
management:
  endpoint:
    health:
      show-details: always

